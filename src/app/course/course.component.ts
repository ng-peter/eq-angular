import { Component } from "@angular/core";

@Component({
  selector: "course-app",
  template: `
    <div class="course-app container">
      <course-list></course-list>
    </div>
  `
})
export class CourseComponent {
}
