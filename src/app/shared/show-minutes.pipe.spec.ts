import { ShowMinutesPipe } from './show-minutes.pipe';

describe('ShowMinutesPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowMinutesPipe();
    expect(pipe).toBeTruthy();
  });
});
